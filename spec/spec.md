# Moodo - 気分優先タスク管理アプリ 要件定義書

## 概要

仕事のタスクに「作業見積もり・めんどくさレベル・期限・重要度」を設定し、「今の気分・空き時間」をもとにAIがおすすめスコアを算出して表示するWebアプリ。ただのTODOリストではなく、そのときの気分や状況を踏まえてタスクを選べるようにすることで、消化作業にならず自分の気分を優先した人間らしい働き方を支援する。

**スコープ**
- やること: タスク管理(CRUD) / 気分・空き時間の入力 / AIによるおすすめスコアの算出と表示
- やらないこと: カレンダー連携 / チーム共有 / 繰り返しタスク / Claude APIのコード組み込み

## 背景・課題

- 既存のタスク管理ツール(Todoistなど)はタスクを羅列するだけで、使いこなしている実感がなかった。タイトルだけ並んでいてもメモ帳と変わらない
- 期限順にただこなすだけでは消化作業になり、仕事が楽しくなくなる
- タスクの所要時間・難易度・期限などの属性を見える化し、そのときの気分や空き時間を踏まえて「自分で選べる」状態にしたい
- 根本の動機: 仕事を楽しくしたい。これがコンセプト「人間らしく働く」に直結している

## 機能要件

### タスク管理

- タスクのCRUD（追加・編集・削除）。追加/編集はモーダルで行う
- タスク属性:
  - タイトル（テキスト）
  - 作業見積もり（時間）
  - めんどくさレベル（楽勝 / 普通 / めんどう / やりたくない）
  - 期限（日付）
  - 重要度（低 / 中 / 高）

### 気分・空き時間の入力

- 画面左上に常時表示される入力欄
- 気分: 1〜10の数値で入力（10が最高、1が最低）
- 空き時間: 分単位で数値入力
- 前回値を引き継ぎ、いつでも変更可能
- 変更時はAI評価用JSONを再エクスポートできる状態にする

### AI評価・おすすめスコア表示

- AI評価は手動フローで運用する（Claude APIのコード組み込みなし）
- 評価フロー:
  1. アプリがタスクデータ＋気分・空き時間をJSONとしてエクスポートする
  2. ユーザーがそのJSONをClaude.aiに渡してスコア評価を依頼
  3. ClaudeがスコアをつけたJSONを返す
  4. ユーザーがそのJSONをアプリにインポートすると、おすすめスコアが反映される
- AI評価前（未インポート状態）のタスクはスコアなしとして扱う
- おすすめスコア(0〜100%)を各タスクに表示
- スコアに応じて色分け:
  - 高（70%以上）: 緑
  - 中（40〜69%）: オレンジ
  - 低（39%以下）: グレー
- 色の閾値は後で調整できる構成にする

### タスク一覧

- 期限順で表示
- タスク0件時・AI評価中のローディング表示は実装者に委ねる

### データ保存

- `tasks.json` ファイルをバックエンドが読み書きする
- localStorageは使用しない

## 非機能要件

- 特になし（個人利用のため）

## 関連ドキュメント

- アーキテクチャ設計（技術スタック・データ設計・AI評価フロー・技術選定の経緯）: `spec/architecture.md`
- インフラ設計: `spec/infrastructure.md`
